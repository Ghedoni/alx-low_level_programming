#include <stdio.h>
/**
 * main - Prints numbers
 * Return: Always 0 (Success)
 */

int main(void)
{
	int start = 48;

	while (start <= 57)
	{
		putchar(start);
		start++;
	}
	putchar(10);
	return (0);
}

